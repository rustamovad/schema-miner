# -- Active LLM Provider (Fill only one block; leave others empty) --

# LLM PROVIDER
LLM_PROVIDER = 'OLLAMA' 
LLM_MODEL = 'llama3.1'                         # Specify the model to use for the selected provider (e.g., gpt-4o for OpenAI)

#OPENAI
OPENAI_API_KEY = 'Your OpenAI API key'
OPENAI_ORGANIZATION_ID = 'Your OpenAI Organization ID'    # Optional, only needed if you have multiple organizations in OpenAI

# SAIA / Any OpenAI-compatible endpoint
# Schema-Miner supports any service that exposes an OpenAI-compatible API.
# Examples: GWDG/SAIA (available in Germany) or OpenRouter (available worldwide).
# Just provide the API key and the base URL for your preferred service.
SAIA_API_KEY = 'Your API key'                              # e.g. SAIA key or OpenRouter key
SAIA_BASE_URL = 'https://chat-ai.academiccloud.de/v1'      # GWDG/SAIA (Germany) | or use https://openrouter.ai/api/v1 for OpenRouter

#OLAMA
OLLAMA_BASE_URL = 'http://localhost:11434'

#HuggingFace
HuggingFace_Access_Token = 'HuggingFace access token'
HUGGINGFACE_USE_LOCAL = True   # Set to True to use a local HuggingFace model, False to use HuggingFace Inference API

# -- Process Configuration --

# -- Process Configuration --
PROCESS_NAME = 'Scientific Acknowledgment Mapping'
PROCESS_DESCRIPTION = 'Extracting structured entities from research paper acknowledgments, including funding agencies, grant IDs, individual contributors, and institutional affiliations.'

# -- Data Path Configuration (Only set the path for the stage you want to run) --

STAGE1_SPECS_PATH = '/home/sanobar/schema-miner/result/stage1'
STAGE2_PAPERS_PATH = '/home/sanobar/schema-miner/result/stage2'
STAGE3_PAPERS_PATH = '/home/sanobar/schema-miner/result/stage3'

# -- Output Path Configuration --

RESULTS_PATH = '/home/sanobar/schema-miner/result'
